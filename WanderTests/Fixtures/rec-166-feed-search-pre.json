{
  "bug": "Feed search opens a generic Discover modal instead of the approved temporary search state",
  "pre_fix": {
    "launcher": "shows the normal Places and People tabs inside a separate full-screen presentation",
    "search": "uses the older live-query Discover screen instead of REC-150 submitted search",
    "dismissal": "uses a toolbar chevron workaround rather than the search state's persistent Back behavior"
  },
  "expected": {
    "launcher": "opens directly into the REC-150 place-search state with the field focused",
    "chrome": "pins Back and the search field at the top while hiding normal Discover tabs and body",
    "clear": "clears results but remains in the teaching search state",
    "dismissal": "Back cancels in-flight work and returns to the prior Feed state"
  }
}
