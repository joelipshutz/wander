# Astir native onboarding verification

## Continuous film archive (T22)

Source `1e27cb4513ee4f55d496ea7554ce2242c81e5610`; local tag `archive/rec-529-film-c-2026-09-18`. C is Joe's preferred exploration, preserved separately from production. Read-only fetch confirmed base equals latest remote main `916a6b7`; the exploration is not merged.

Film motion previously shared the reading timer's transition pause. It now runs independently while slides travel, with existing user pause, background, accessibility and account-entry stops preserved. **24 tests passed (22 unit, two UI, zero failures/skips)** in `native-film-continuous-tests.xcresult`; both film treatments exercise automatic playback and live Next/Log in/email/verification interactions. Both full recordings independently reach signup.

The before/after transition recording check finds 1.1 seconds of near-frozen texture before, none after at 100 ms sampling in the same stationary header region. This checks continuity, not device frame-rate performance. Full native frames were visually inspected. Browser archive playback, Focus and transition shortcut passed. Approved A remains its earlier capture; B/C source hashes and movie hashes are in the pinned archive manifest.

[Preserved archive](opening-film-archive.html?v=1e27cb4) and [restart notes](archives/film-c-2026-09-18/README.md). No merge/release or new Higgsfield generations. N08-N12 remains next; Ryan retains NUX.

## Current Events-reference refinement (T21)

Native source `04667200de81b0eef066644e347989145941ee9a`. Read the selected Events 03C revision-4 brief, shader/composition and actual bundled movie. Native B/C now include warm registration smear, irregular density dropout, sparse fine wear, row displacement and three short major tracking windows per eight-second cycle. C's Connect with your is Helvetica Neue Bold Italic; changing words and benefit lines remain condensed. Approved A, copy and slide pacing are preserved.

**26 focused tests passed: 22 unit + four UI tests**, zero final failures/skips. `native-film-distortion-playback-tests.xcresult` has 25; `native-film-auto-only.xcresult` has the explicitly discovered automatic test, which runs both film modes through hittable signup. Both fresh full-phone recordings also reach account creation. The first combined test selection omitted the newly added automatic test; only the later isolated result is counted for it.

The initial overlay hit-testing regression and a full-speed RenderBox crash were reproduced and fixed before these final checks. Effects now disable interaction at their common parent; native text filters do not wrap AVPlayer or material-backed mastheads. Failure evidence remains separate. Full-state contact sheets, per-take hashes, source commits and capture endpoint proof are retained. Browser playback/pause, complete signup frames, C's italic lead and the embedded original Events reference passed; no media/console errors observed. Physical-device pacing and additional native device sizes are not newly verified.

[Updated Swift comparison](opening-film-comparison.html?v=0466720). A remains sourced from `6432401`; B/C are `0466720`. The first film comparison is preserved with its original manifest. Zero new Higgsfield generations. Local only; no merge or release. Existing iPhone 16e is left paused on C with the Style menu, per Joe's request for simulator review. N08-N12 follows this checkpoint; Ryan owns NUX.

## Current film comparison (T19)

Native source `64324018242bc2c20db9f66b31fd28fa96607f9a` adds two optional debug film treatments beside the approved opening. Matching type uses Helvetica Neue Condensed Black for Connect with your, changing words, benefit headlines and the account title, with italic supporting copy. The original Coming Soon texture is reused; **zero new Higgsfield generations**. Native controls retain their actions; automatic motion stops at account entry. Release/default launches keep the approved opening.

**22 tests passed: 19 unit tests and three UI tests**, zero failures or skips, on iPhone 16e / iOS 26.3.1. The film UI test exercises both treatments. Coverage includes preserved sequence/pacing, fonts/resources, Next, signup without X, login, email verification and background recovery. This clears the formerly pending no-X native check. Evidence: `native-film-final-tests.xcresult` and matching log. No new full-suite or physical-device performance claim.

Four full native recordings (approved dark/light, film, film + matched type) independently reached Create your account. Their text checks and screenshots are in `native-captures/film-exploration/`; source and SHA-256s are in `native-film-comparison.json`. Full-state sheets are in `native-board-qa/*-film-states.jpg`. Browser playback, pause, seeking, complete account endpoints, focus, TV and appearance switching passed. Video range requests return 206; no browser errors observed. Browser duration omits some of the raw static tail, but endpoints show complete signup.

Native Style switching, Next and replay were manually verified. Xcode Branch Chooser shows `codex/rec-529-onboarding-film-exploration`. The existing iPhone 16e is left open for Joe's requested review with local test authentication and the Style menu. Additional device sizes and physical-device frame pacing remain unclaimed. No other simulator was stopped.

Local review: [opening-film-comparison.html](opening-film-comparison.html?v=6432401). Public draft creation and the Linear handoff comment were rejected by automatic approval review as external disclosures. Approval is pending; nothing pushed or merged. The durable local ledger has the full restart state. NUX remains Ryan's work.

## Previous shipping checkpoint (superseded for no-X native validation)

**Current shipping status, September 18:** approved Signal opening merged in #668; signup close removal merged in #670. The final opening passed 240 unit tests, four native UI journeys and Simulator build. The one-line signup correction has source/required-CI validation; fresh native build/tests and captures were blocked before compilation by the disk gate. Later account-setup evidence below belongs to the preserved review branch, not current main. Ryan now owns post-onboarding NUX. Use [T18 in TASKS.md](TASKS.md) for the active queue and [the final opening report](native-board-qa/native-opening-final-report.json) / [signup correction report](native-board-qa/native-signup-no-close-report.json) for current shipping evidence. Earlier checkpoints below remain historical.

Current checkpoint: September 17, 2026. [REC-529](https://linear.app/recme/issue/REC-529), branch `codex/rec-529-native-onboarding-review`, based on `4937033a99175b2b22b48440a1376b644f6ad8e6`. Implementation is in [the isolated native worktree](</Users/joelipshutz/Documents/ChatGPT/New project/wander-native-onboarding-review>). This checkpoint replaces earlier build/test status; it does not assert a merge, release, App Store submission or production account/backend change.

**Current T15 Signal slide checkpoint:** Native source `966866bac8f31fd13429a1aae22c7719c381da9c` replaces the active physical boards with ONE plain-text treatment. COMMUNITY → PEOPLE → PLACES → LOVED ONES uses naturally spaced Avenir Next Bold Signal caps. The external Connect with your remains editorial serif. Whole outgoing/incoming labels slide simultaneously using shared 1.5-second easing; the first word has its own entrance plus a full reading hold. The quick serif fade precedes the three-line A / LOCAL / EXPERIMENT slide. Complete benefit phrases and upper native previews use the same motion. No flap renderer or flap haptics run in the current welcome path; archived physical helpers remain inactive.

The shared-helper build passed **29 welcome unit tests and three native UI journeys**, zero failures (`native-signal-slide-tests.log/.xcresult`). Both complete native light/dark recordings reach account creation. Six compact native screens and full-size settled/mid-transition frames were inspected: exact copy, serif lead, actual park photo, whole-word handoffs, full-scene move and simultaneous benefit UI/text move. The one-player gallery supports play/pause, restart, state seeking, immediate light-mode playback, account-end seeking and TV mode. Normal, wide-TV and narrow layouts have no horizontal overflow; complete phones use contain without cropping. The final version resets the old document’s pinch-zoom offset; mouse and keyboard controls pass. Browser errors: none. Both movies support HTTP206 and match their hashes; all 27 source hashes match. Both owned simulators are shut down. Evidence: `native-board-qa/native-signal-slide-report.json`.

Canonical review: `http://127.0.0.1:8766/session-2026-09-16/opening-explorations.html?v=966866b`. W00–W02, the four-state brief, source inventory and full flowchart are refreshed. Previous physical gallery preserved at `archives/native-finishes-readable/`. T15 exact transcript and the durable checklist are at the top of `TASKS.md`. **Stop at this launch-blocker checkpoint for Joe/Ryan feedback.** Events-placeholder style/context was explicitly deferred and has not been searched or inspected. Separate NUX work remains untouched. No Higgsfield generation or credits used in this pass.

Earlier unrelated place-data performance failures and broad Map/NUX gates remain unresolved; this is scoped creative review, not release clearance. Physical-device frame pacing is not measured. Local code/evidence is ready; existing public GitHub and detailed Linear publication choices remain pending and were not retried. Free space after the final build was 47.6 GiB; a future build must pass the helper’s 50 GiB floor. Do not bypass it or delete unrelated caches.

**Previous physical/readability revision:** native source `d4225f5b58967833a9554ad5320f3b93eb847ade` keeps the approved large Avenir Next Bold opening caps, uses Astir’s editorial serif for “Connect with your,” and changes benefit boards from 17 to 13 columns. Benefit caps are roughly one-third larger, with unchanged copy, three rows and photo dimensions. Physical leaves have visible edges, hinges, moving shadows and independent fast chatter that slows over the last three turns. Upper slides and independently driven benefit flutters each take 1.5 seconds.

The current build passed **28 welcome tests and three native UI journeys** (`native-physical-readable-tests.log/.xcresult`). The preceding physical implementation passed all 2,003 units initially; a later full run and isolated retry retained unrelated place-data performance budget failures (warm reads 123.6ms, then 132.3ms against 100ms; list suggestions 766.0ms against 750ms). Those remain unresolved. No clean final full-suite or broad Map/NUX clearance is claimed.

All six final native movies in `native-captures/finishes-readable/` reach account creation. Full-phone frames, all 12 settled benefit frames, and compact opening/Places/People in light and dark were directly inspected. OCR splits some caps at the hinge, so exact words were confirmed visually. Review bookmarks use settled Places at +21s and People at +30s; slide playback begins at +15.3s before the transition. The gallery loads and plays all six movies, pauses correctly on the final phrase, seeks to account screens and supports individual play/pause. Whole phones remain contained above the transport at the normal window size, without crop transforms or horizontal overflow. No browser errors; all six movies support HTTP 206 seeking. All 27 source hashes and six movie hashes match. Both task-owned simulators are shut down. Evidence: `native-board-qa/native-readable-report.json`.

The full board, W00–W02, four-state brief and first Station preview are refreshed. Exact T13/T14 feedback and both T14 reference screenshots are preserved. The accepted first physical Station take is archived under `archives/native-physical-first/`; the previous complete gallery is in `archives/native-finishes-independent/`. Physical-device haptic sensation/frame pacing remain for review. Current code and handoff stay local because public publication awaits the existing approval choice. Separate NUX work and the broader task register are untouched.

**Previous independent-flutter correction:** native source `2ef36f9312639b712673481d1d1b7a0ece1904d5` restores dark faces on dark app screens and light faces on light app screens. Each of the 30 opening cells has independent start timing, turn durations, letter paths and landing times, with 12–16 faster turns inside the existing 1.5-second flutter. This fixes reuse of a column index across rows. Plans are cached per word change; pause/scrub remains deterministic. All three finishes keep natural Signal caps, three visible rows and the external lead-in.

All **2,001 unit tests**, including 26 welcome checks, and **three affected UI tests** passed without retry or relaxed thresholds. Automatic progression: 39.991s; manual paging: 21.587s; signup/close/login/close: 13.558s. Evidence: `native-independent-flaps-tests.log` and `.xcresult`. Previous timing failures remain historical; this pass is green. The separate broad Map UI gates and NUX implementation gaps remain outside this clearance.

All six replacement recordings in `native-captures/finishes-independent/` reach signup and show actual Swift animation. Full-phone opening/flutter/final/benefit frames and compact dark/light layouts were inspected. Native text identifies per-take word, settled final phrase, Places and People bookmarks. A People OCR omission in light mode was inspected directly: the native line correctly reads YOU LOVE. Final Graphic light is likewise visually verified at the settled bookmark.

The gallery loads and plays all six recordings, supports individual play/pause and account seeking, and preserves `object-fit: contain` without crop transforms. State playback begins before the lead-in fade and scene slide. The small-window fit now accounts for differently wrapped card headers so every phone remains above the fixed transport. TV geometry, narrow stacking and no horizontal overflow passed; native account screens in both appearances and the normal-window framing were visually checked. No console errors; local video requests return HTTP 206. Evidence: `native-board-qa/native-independent-report.json`, with all 27 source hashes and six media hashes verified. Both task-owned simulators are shut down.

The exact T12 excerpt and current understanding are saved at the top of the four-state brief. The previous eight-turn/inverted-face gallery remains in `archives/native-finishes-full-board/`. Physical-device frame pacing and haptic sensation still need iPhone review. Public pushing remains pending the existing approval choice; this completed local revision does not claim a public PR update.

**Previous full-board refinement:** native source `b7ff1f42f95c21efe5980b38b0996101adc6a0f8` uses three visible rows of ten cells, an external lead-in, and eight flips over 1.5 seconds for every opening cell, including repeated letters and blanks. In light mode the faces are black; in dark mode they are white/off-white; Signal letters stay constant. All three finishes and benefit boards follow this contrast rule. Four haptic impacts and the scene timing remain unchanged.

Six new recordings in `native-captures/finishes-full-board/` each reach account creation. Native frames show the whole phone, all three opening rows, fluttering blanks, the final phrase, actual Places/People previews and signup. Compact dark/light screens were inspected. The gallery now defaults to full phones and retains the complete screen during play, pause, seek and state selection. TV sizing, all-six playback/account seeks, native-text-confirmed state bookmarks and error checks passed. Source hashes for all 27 files and six video hashes match. Evidence: `native-board-qa/native-full-board-report.json`. Both owned capture devices are shut down.

Validation is scoped: 25 welcome unit tests and 147 navigation contract checks passed. Manual paging and account entry/close passed; automatic progression passed its unchanged isolated recheck in 53.242s. The first automatic test remained paused after its Play tap (recorded UI hierarchy), rather than entering the timed flow. The first broad unit run exposed a stale source-string assertion for the earlier auth-close fix; that assertion now checks the close action independent of formatting. The following 2,000-test run retained a separate place-category 0.1s timing failure (0.1213s; isolated recheck 0.1022s). No clean full-suite or physical-device pacing claim is made. Logs/results: `native-full-board-tests`, `native-full-board-verified`, and `native-full-board-recheck`. Existing broad Map gates and NUX gaps remain.

**Publication:** Source and evidence are saved locally through `4ddddfb`; the public PR remains on `2c10e7c`. Automatic approval review blocked publishing internal review notes to the public repository. This does not block the completed local review.

**Previous analog refinement:** native source `39214cb28dc34ef321d43bd393ec10b391647178` moves “Connect with your” outside one visible word row. Seven flips over 1.5 seconds, fixed hinge details, a weighted landing/rebound and four native haptic impacts replace the earlier short flutter. A 180ms lead-in fade precedes the final three-row phrase; the opening ends at 15.6s. The natural monospaced glyph metrics are preserved. All **26 focused unit tests** and **three final UI journeys** passed; quick signup-close-login also exposed and fixed a retained finished state. These are focused results, not a new full-suite claim.

All six actual Swift recordings in `native-captures/finishes-analog/` reach account creation in native and browser verification. The published gallery supports direct word/fade/final/slide review, dark/light, full-phone framing, letter crops, seek/play/pause, focus and narrow stacking. No browser errors were observed. Shared Station typography/layout was inspected on compact light/dark screens. Evidence: `native-board-qa/native-analog-report.json`, `native-analog-final-tests.xcresult` (26 unit passes and reproduced auth bug), and `native-analog-auth-reset-tests.xcresult` (three final UI passes, 81.760s). Both task-owned capture devices are shut down.

The first Graphic dark capture stopped on People; it is retained as `graphic-dark-incomplete.mp4`, not offered as a complete flow. The replacement reaches account creation. The cause of the first failure is not established. Raw captures retain actual simulator timing and differing load delays/static tails. Physical-device motion and haptic feel still need iPhone review.

One authorized eight-second Higgsfield study was generated. It introduced extra rows and wrong words, so it remains a labeled reference only; the app contains no generated video. The NUX transcript re-audit in `brief/nux-transcript-spec.html` corrects prior native completion claims: Nearby Places, Check In/Wanna annotations, the three-purpose Lists explanation, populated location-aware Map demo, Feed scroll reveal, finale choices and device-feature motion remain work to do.

**Earlier typography correction:** Joe rejected the condensed, horizontally squeezed caps in the first three-finish pass. The native source now uses bold monospaced letterforms at a uniformly fitted font size; no horizontal-only glyph scale is applied. All finishes share the same glyph metrics. Corrected source is `7a5125f8999000925d2cb61e90172f57e33abb88`. The simulator build and **22 focused welcome/photo unit checks passed**, 2.095s test time (2.105s wall), in `native-natural-type-tests.xcresult`. The full-size native opening was visually inspected for natural letter proportions. Updated media uses `finishes-natural-type/`; the original condensed take remains archived. All six replacement recordings reach account creation in native and browser checks. The shared corrected font was inspected on compact light/dark screens. Large letter inspection, full phone playback, seek/pause, focus, fullscreen, keyboard controls and three vertically accessible previews at narrow width passed with no missing assets or JavaScript errors (`native-board-qa/native-natural-type-report.json`). The published review now uses this corrected set.

**Prior material pass, with rejected typography:** source commit `61749886a7af1b1a80333c942356503dc03bb7fa` implements Station, Sculpted, and Graphic in actual Swift. The same uppercase Signal copy/grid uses cached native glyph faces and retained tile layers, with a shared 0.6-second slide curve. No Higgsfield generation ran. Debug-only `WANDER_ONBOARDING_FLAP_FINISH` selects a treatment; release defaults to Station pending creative review.

The simulator build and **22 focused unit tests passed** (0.845s test time); new pixel-render checks cover the settled final board, appearance/finish reuse and resizing. Manual paging passed in 32.499s and rapid Next/signup/login in 28.102s. Automatic progression initially missed its page-two observation during slow simulator startup; starting observation from the real Play control passed unchanged timing limits in **54.004s**. Evidence: `native-three-finishes-tests.xcresult` (22 units and two passing UI journeys; initial auto failure retained) and `native-flaps-auto-test.xcresult` (successful automatic journey).

Six complete raw simulator recordings are identified in `native-finishes.json`; each account end screen was checked from the simulator. `opening-explorations.html` is the new three-finish gate, including light/dark, letter zoom, individual focus and shared playback controls. Recordings preserve actual app timing and only skip launch footage at playback. They are not HTML approximations. Functional test success does not establish physical-device frame pacing. Initial short recordings that ended before signup were replaced, not presented as complete. Browser checks passed with no JavaScript errors or missing assets: all six movies play/seek through signup, appearance switches, zoom, focus, keyboard controls, full screen and mobile width work (`native-board-qa/native-finishes-report.json`). All six compact native layouts were inspected (`finishes-compact-native.jpg`). Raw movies required byte-range HTTP support; the original generic server exposed a zero-length seekable range. `../serve_review.py` now serves HTTP 206 correctly and seeking is verified. This server fix does not by itself establish native frame pacing.

**Prior uppercase baseline (historical):** final simulator build and all **1,992 unit tests passed** (32.572s, 33.117s wall), plus **three affected welcome UI tests passed** (64.553s). Unit evidence: `uppercase-stable-ui-tests.log` and matching `.xcresult`. Final preview-preloading and animation-completion UI evidence: `uppercase-completion-ui-tests.log` and matching `.xcresult`. A preceding unrelated performance outlier measured 0.123681s against 0.1s; it passed both its isolated recheck and this complete rerun without changing the threshold.

The opening now uses three fixed rows of uppercase Signal letters, white/light or dark faces, two flips per change, A.03 near Next, and **A / LOCAL / EXPERIMENT** on separate rows. Actual Hotchkiss Park photo was retrieved through the app’s place-photo endpoint and bundled with attribution for signed-out use. The production preview/photo pipeline renders it. Both upper native views remain mounted during the Places → People slide; the lower board stays anchored and flips. Rapid Next taps are preserved and account entry visibly slides both screens. Cleanup waits for SwiftUI’s actual animation completion, preventing the outgoing Map from disappearing early under rendering load.

Prior baseline timing: **1.8s hold + 0.6s flips = 2.4s per word**, description at **3.6s**, final settled at **9.6s**, opening slide at **12s**, **0.65s** slide duration, and **7s** per benefit. A.03 stays through the final phrase; this persistence and exact cadence remain creative review choices. Prior A/B/C recordings and HTML explorations are preserved in [Archives](archives.html). Other onboarding work is deferred for this refinement, with its full register retained in TASKS.md.

**Published review:** final commit `44376daf78d1a747b8ba3d1260e771f9d181ba6e` is pushed to draft PR #648. The [dark/light opening gallery](opening-review.html), four native states and full board use the latest simulator captures. Exact frames confirm the outgoing Map remains visible throughout the light/dark slide and both old/new account compositions move. TV fit, mode selection, replay/pause/seek, complete account end frames, latest brief and Archives links passed (`native-board-qa/uppercase-report.json`), with no JavaScript errors or missing links. Browser durations are 42.985s dark and 29.916667s light; both reach the full account screen. AVFoundation also reports the light recording’s static tail, which Chrome omits. No app motion or screen pixels were reconstructed. Compact evidence is from the stable-panel build before lifecycle-only preloading/completion edits.

The detailed first-pass evidence below is historical where it refers to 1,989 tests, five flips or A/B/C media. The latest light/dark media and source fingerprints supersede that opening evidence; unchanged downstream flow evidence remains valid.

[native-copy.json](</Users/joelipshutz/Documents/ChatGPT/New project/onboarding-copy-review/session-2026-09-16/native-copy.json>) contains 41 source-audited copy cards, including the confirmed opening, actual OS permission text, recovery states and conditional purposes. Media evidence is identified explicitly; an uncaptured conditional card is not presented as a rendered screen.

## Historical implementation and verification

| Area | Current behavior | Verification |
| --- | --- | --- |
| Confirmed opening | Current source keeps the lead-in fixed while **community / people / places / loved ones** change through native **per-letter mechanical hinged flips**. The whole headline ultimately resolves to **“a local experiment”**. Supporting text and whole-scene slides remain distinct from letter motion. | Current build, all 1,989 unit tests and three affected welcome UI tests passed. A/B/C native frames were visually verified for hinged halves, staggered cycles, every word, final headline and loaded Map → People → signup, then published. New compact opening is readable. Source refs/hashes are unchanged; cadence and supporting-copy details remain creative review choices. |
| Welcome and auth | Native MapKit/activity benefits, three-page progression, direct Log in, native account entry and verification/background recovery. Signup Log in now has a full-width 44pt minimum hit area. | **Quiet full native auth journey passed in 41.611 seconds** on the compiled flicker build (`auth-20260917T102024`), including preserved verification through backgrounding. The prior measured text-only hit height was about 20.7pt; it is now 44pt. Callback wiring was already correct: this is a hit-area improvement plus reliable UI test helpers/quiet execution, not a claimed callback-bug fix. Local fixture auth is not proof of new live Apple/Google account access. |
| Profile and crop | Shared live profile header binds name, @handle and photo. Required photo save blocks progression on upload failure; unfinished legacy profiles require identity proof, completed users remain ready. Native crop toolbar keeps Cancel/Choose inside the safe area. | **Real profile/crop flow passed in 79.583 seconds on a prior build of this unchanged flow**, including actual photo selection, native crop toolbar and successful Choose. Identity validation, upload-failure and offline/fetch-failure checks passed. Crop title/action strings remain Crop photo / Cancel / Choose / Saving…. |
| People | Real name/username member search; immediate per-row Follow → Following with pending/retry protection. Nothing is preselected or batch-followed by Continue. | Native Follow journey passed on a prior build of the unchanged flow. The late-search unit test explicitly waits for its stub to start; behavior assertions are unchanged and the complete current 1,989-test suite passed. N33/N34 accepted empty/error text is retained; sample members are labeled. |
| Location and Contacts | N10: “Connect with your people” / “Use your contacts to connect with people you know.” N36: “Astir uses your contacts to help you connect with people you know.” Neutral permission sequencing and local invitation-selection data flow are unchanged. | **Actual N30 → N31 location-denial recovery and N36 Contacts-denial journey passed.** New N36 purpose matches project.yml, rebuilt Info.plist and the real OS dialog. OS-owned headings/buttons were independently read from native captures and entered into the copy inventory. No automatic Contacts-to-member matching is claimed. Historical Apple evidence did not mandate the exact removed disclosure sentences. |
| Notifications | Three native example cards animate in the primer and contextual campaigns with lifecycle cancellation and Reduce Motion. Existing eligibility/caps remain intact. | **Actual N12 system request → N32 denied/Settings recovery passed.** Native system text is verified. Ryan/Mina are fictional examples. N28/N29 are suppressed only when OS authorization and the app push preference are both enabled; one lifetime impression per trigger, three per campaign. No delivered notification is implied. |
| Primary Map overview | M01–M06 explain actual Featured, Friends, More, Search, + and pin rings; controls remain usable. Finale preserves the existing Bourdain baseline. Forced Add/save demo and automatic sample save are removed. | Actual native overview, filter-action exit and finale-return tests passed in the earlier selected run. **Final main M01–M06, N25 and C01 and compact QA are visually verified.** All three affected welcome tests passed after the flicker change. Earlier Map/Feed selected tests and visual evidence remain valid; no broader Map timing failure is silently cleared. |
| First-use and return guidance | Account-scoped new-user hints on Add/import, Feed, Lists and place actions; five-second/Next dismissal without a blanket gesture. Enrollment survives primary completion. N26 is retired; N27 third-physical-launch guide remains. | Enrollment, account switching, legacy migration, retirement and launch/checkpoint tests passed. Feed/Lists hints and Feed activity expansion passed. Established accounts do not acquire a tour from a global feature flag. The existing guide remains at `https://getrec.me/extensions`; a guide redesign is separate work. |
| Other conditional NUX | Username states, recovery, native launch/loading and Camera/Calendar/Photos purposes remain explicitly listed. | N35 represents username/saving variants; N67 is the actual crop screen. Camera, Calendar and save-to-Photos are conditional follow-on capabilities, not required primary onboarding steps. Their purpose text is source/bundle evidence, not a claim that every corresponding system dialog was captured. |

## Historical first split-flap validation

The actual split-flap build/test command exited 0 with **TEST SUCCEEDED** in [native-split-flap-tests.log](</Users/joelipshutz/Documents/ChatGPT/New project/native-onboarding-build/native-split-flap-tests.log>) and its matching `.xcresult`, completing September 17 at 11:10:06:

| Check | Final result |
| --- | --- |
| Complete unit suite | **1,989 tests, 0 failures**, 41.869 seconds (43.117 seconds wall) |
| Three affected native welcome UI tests | **3 tests, 0 failures**, 59.523 seconds |
| Automatic welcome progression | Passed, 33.072 seconds |
| Manual page navigation/actions | Passed, 13.583 seconds |
| Next → signup → Log in flow | Passed, 12.868 seconds |

This was the first mechanical split-flap result; the prior 1,984-unit/three-UI opacity-pulse pass is superseded for current build validation. All four glyph tests and the deterministic late-search-startup test are included in the current complete unit pass. Earlier selected validation also passed nine UI tests including Map and Feed coverage. **Earlier broad Map timing/selection failures are not cleared by these selected runs; no full broad UI-suite pass is claimed.**

The full native auth test `testWelcomeToLoginVerificationSurvivesBackground` **passed in 41.611 seconds** in [auth-20260917T102024/test.log](</Users/joelipshutz/Documents/ChatGPT/New project/onboarding-copy-review/session-2026-09-16/native-test-results/auth-20260917T102024/test.log>). This quiet run used the prior opacity-pulse build and preserved the verification flow; auth implementation is unchanged in the split-flap build. native-auth.mp4 contains that successful prior-build recording. Signup Log in hit area was enlarged from the measured approximately 20.7pt to a full-width 44pt minimum; callback wiring was not changed. Test helpers and controlled execution resolved unreliable UI targeting/waits. Do not describe this as a callback bug being fixed.

Other actual native results remain evidence for unchanged flows, executed on prior builds: Location/Contacts denial recovery (`permissions-20260917T093708`), notification request/denial/Settings recovery (`notification-permission-20260917T094838`), per-row search/follow, and required profile/photo/crop (`profile-20260917T095538`, **79.583 seconds**, successful Choose and enabled Continue). Earlier broad UI runs had Map timing/selection failures; no full broad UI pass is asserted.

## Rendering and media fidelity

The **first native mechanical split-flap pass** is published, recorded in [split-flap-review-ready.json](</Users/joelipshutz/Documents/ChatGPT/New project/onboarding-copy-review/session-2026-09-16/native-captures/split-flap-review-ready.json>) with movie hashes. Root visually verified A/B/C native frames for mechanical glyph halves and staggered cycles, initial lead-in/community, all word changes, final headline and real loaded Map → People → signup. Launch lead-in trims are A **2.0s**, B **2.0s**, C **1.8s**. W00 and V01 were replaced from A; four state PNGs use raw A at **2.5 / 7.5 / 13.5 / 18 seconds**. W00-compact is a new actual native screenshot checked for readability. The media manifest is refreshed and the brief, gallery and full board are published. These are native frames; state numbers organize the review rather than reproduce verbatim numbered requirements. Cadence remains a first pass for creative review, and final browser QA passed separately.

The iOS 26.3 simulator was missing `AppleColorEmoji.ttc`, producing LastResort boxed glyphs in both older baseline captures and native Map annotations. The production fix shares one cached capability/category resolver between SwiftUI category views and the UIKit annotation renderer. It uses existing native category SF Symbols only when emoji rendering is unavailable; healthy devices keep emoji. Existing pin discs, colors, rings and image caching are preserved. The fix compiled, all four focused tests passed, and final main/compact captures visibly show proper symbols. No screenshot pixels were reconstructed or patched.

[Compact QA](</Users/joelipshutz/Documents/ChatGPT/New project/onboarding-copy-review/session-2026-09-16/native-captures/compact-qa.md>) records the final N04, N10, M01 and N25 visual checks with capture timestamps, dimensions and hashes. Signup logo contrast is clear, exact concise Contacts copy fits, the Map coach fits, and the full finale title/quote/closing/Skip fit. Previously verified N12 dark appearance remains valid with unchanged source.

The board labels sample activity, local member fixtures and fictional notifications. Map captures use explicitly simulated Los Angeles coordinates (`34.075, -118.285`), not the user's real location. Place actions remain data-dependent. For C04, the native capture route selects the real demo fixture using `-WanderMapPlace "Bar Nido" -WanderMapSheetExpanded`; merely expanding the sheet does not select a place. Bar Nido has the fixture phone/website; unsupported actions are not fabricated.

## Remaining handoff work

Final browser QA passed all three videos' playback/seeking through signup, Focus/Compare, four full-width brief images, and main V01 playback/pause/seek, with no JavaScript errors or missing assets (`final-split-flap-report.json`). Current tests and native media verification/publication are complete; exact cadence remains for creative review.

1. Review the native opening takes and four-state brief, then apply copy/motion feedback on the same branch. Draft PR #648 is open; broader Map UI failures remain a pre-merge gate. Existing auth, profile, Follow, permission and Map evidence retains its prior-build scope.

Other creative alternatives and future functionality remain in [TASKS.md](</Users/joelipshutz/Documents/ChatGPT/New project/onboarding-copy-review/session-2026-09-16/TASKS.md>). They are not missing-word blockers or additional completion requirements for this native implementation checkpoint. No deployment or App Store action is authorized by this document.

## Current review server

From the `onboarding-copy-review` directory run `python3 start_review.py` to start or reuse the detached loopback server. Keep byte-range support for raw video seeking. The main and prior review URLs still work; the current gate is `/session-2026-09-16/opening-explorations.html`.
